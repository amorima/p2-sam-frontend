name: deploy frontend
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: [self-hosted, frontend]
    steps:
      - name: atualizar código
        run: |
          cd /home/antonio/p2-sam-frontend
          git pull origin main
          npm install
          export NODE_OPTIONS="--max-old-space-size=4096"
          npm run build
          pm2 restart frontend
